import 'package:flutter/material.dart';

class MainMeanDetailPageView extends StatelessWidget {
  const MainMeanDetailPageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: (Text("1")),
      ),
      body: Center(
        child: Text("1"),
      ),
    );
  }
}
